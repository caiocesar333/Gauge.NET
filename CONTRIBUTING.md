# Contributing

## Requirements
- .NET SDK 9.x

## Build
dotnet build -c Release

## Test
dotnet test -c Release

## CLI (local)
dotnet tool restore
dotnet gauge --help

## PRs
- Keep PRs small and focused
- Add/update samples and traces when changing validation behavior
